To enable .ly support for the .gpp and .ly files just add the filetype.vim file into the ~/.vim folder.

I still did not find how to work correctly with the 'modeline' feature of vim in order
to place modelines in the .gpp files themselves so that I would not need the plugin.
