{% set lilypond_version = shell_output(command='lilypond --version 2>/dev/null | head -n 1 | cut -d " " -f 3', depends_on=[]) %}{% set date = shell_output(command="date +%d-%m-%Y", depends_on=[]) %}




% lets emit the definitions
{% include "src.tera/include/common.ly.tera" %}

% lets always include guitar definitions
\include "predefined-guitar-fretboards.ly"



% from here everything needs to go into a loop
{% set meta = load_toml(path="out/derived/openbook/nows_the_time.toml") %}
% include anything the user wants before the bookpart starts
{% set part = "Defs" %}{% include "src.tera/openbook/nows_the_time.ly.tera" %}


% this causes the variables to be defined...




{% set part = "Vars" %}{% include "src.tera/openbook/nows_the_time.ly.tera" %}

% now play with the variables that depend on language



% calculate the tag line


% calculate the typesetby









% taken from "/usr/share/lilypond/2.12.3/ly/titling-init.ly"
\markup {
	\column {
{% if meta.has.dedication %}		\fill-line { "{{ meta.attributes.dedication }}" }
{% endif %}		\override #'(baseline-skip . 3.5)
		\column {
			\huge \larger \bold
			\fill-line { \larger "{{ meta.attributes.title }}" }
{% if meta.has.subtitle %}			\fill-line { \large \smaller \bold \larger "{{ meta.attributes.subtitle }}" }
{% endif %}{% if meta.has.subsubtitle %}			\fill-line { \smaller \bold "{{ meta.attributes.subsubtitle }}" }
{% endif %}			\fill-line {
				"{{ meta.computed.fullpoet }}"
				"{{ meta.computed.fullcomposer }}"
			}
			\fill-line {
{% if meta.has.piece %}				"{{ meta.attributes.piece }}"
{% endif %}{% if meta.has.opus %}				"{{ meta.attributes.opus }}"
{% else %}				""
{% endif %}			}
		}
	}
}
\noPageBreak


% include the preparatory stuff, if there is any

% calculate the vars



% score for printing
\score {
	<<
\new ChordNames="Chords"
	\with {
		\remove "Bar_engraver"
	}
% # transpose with 'inline' is true!
	\transpose c c { {%- set part = "ChordsFake" %}{% include "src.tera/openbook/nows_the_time.ly.tera" %}}
% this thing will only engrave voltas. This is required to put the volta under the chords.
% No great harm will happen if you don't put it, only the voltas will be above the chords.
%\new Staff \with {
%	\consists "Volta_engraver"
%}
\new Staff="Melody" {
\new Voice="Voice"
% # transpose with 'inline' is true!
	\transpose c c { \relative c'
	{% set part = "VoiceFake" %}{% include "src.tera/openbook/nows_the_time.ly.tera" %} }
}
	>>
	\layout {
	}
}
% score for midi
\score {
	\unfoldRepeats
	<<
\new ChordNames="Chords"
	{% set part = "ChordsFake" %}{% include "src.tera/openbook/nows_the_time.ly.tera" %}
\new Staff="Melody" {
\new Voice="Voice"
	\relative c'
	{% set part = "VoiceFake" %}{% include "src.tera/openbook/nows_the_time.ly.tera" %}
}
	>>
	\midi {
	}
}


\noPageBreak
\markup \column {
	% just a little space
	\null
	\fill-line {
		\smaller \smaller { "{{ meta.computed.copyright }}" }
	}
{% if meta.has.copyrightextra %}	\fill-line {
		\smaller \smaller { "{{ meta.attributes.copyrightextra }}" }
	}
{% endif %}{% if meta.has.copyrightextraextra %}	\fill-line {
		\smaller \smaller { "{{ meta.attributes.copyrightextraextra }}" }
	}
{% endif %}
	\fill-line {
		\smaller \smaller { "Typeset by {{ meta.attributes.typesetter }}, Built at {{ date }}, Engraved by lilypond {{ lilypond_version }}" }
	}
	\fill-line {
		\smaller \smaller { \with-url #"https://veltzer.github.io/openbook" https://veltzer.github.io/openbook }
	}
}





